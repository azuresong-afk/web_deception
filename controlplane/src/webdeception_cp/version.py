"""Версия сборки control plane."""

# Подставляется при сборке образа. Значение по умолчанию означает запуск
# из исходников, а не из релиза.
#
# Как и в сенсоре, версия не попадает ни в один HTTP-ответ: название и версия
# продукта в ответе — готовый признак для опознания (угроза T5).
VERSION = "dev"
