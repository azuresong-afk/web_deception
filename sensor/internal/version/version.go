// Package version хранит версию сборки сенсора.
package version

// Version подставляется при сборке флагом -ldflags и по умолчанию означает
// сборку из исходников, а не из релиза.
//
// Версия намеренно не попадает ни в один HTTP-ответ. Заголовок или строка
// с названием и версией продукта — это готовый признак для атакующего,
// по которому он опознаёт нашу защиту и обходит приманки (угроза T5).
// Версия нужна только в логах и только для нас.
var Version = "dev"
