# Модель угроз

Версия 1.4, этап 1. Метод: STRIDE. Пересматривается при добавлении компонента,
новой внешней интеграции или изменении границ доверия.

Изменения в 1.1: исходный код продукта опубликован (ADR-0011) — уточнена мера
к угрозе T5 и добавлен принятый риск R4.

Изменения в 1.2: добавлена угроза T14 — небезопасные настройки сторонних
образов; найдена на шаге 4 в конфигурации NATS по умолчанию.

Изменения в 1.3: меры к T12 дополнены правилами CI (ADR-0015).

Изменения в 1.4: меры к T1, T4, T7, T12 подкреплены автоматическими проверками
(ADR-0016); добавлен принятый риск R5 — уязвимости без исправления в базовых образах.

## Активы

- Трафик клиентов, проходящий через сенсор (пароли, токены, персональные данные).
- Доступность защищаемого приложения.
- Целостность конфигурации сенсоров.
- Журнал событий и профили атакующих.
- Секреты продукта: ключ подписи конфигурации, HMAC-ключи canary-токенов,
  токен Telegram-бота.
- Репутация продукта.

## Границы доверия

1. Интернет → сенсор. Весь вход враждебен.
2. Сенсор → control plane. Сенсор работает у клиента и может быть скомпрометирован.
3. Control plane → администратор (дашборд, Telegram).
4. CI/CD → артефакты (образы, релизы).

## Угрозы и меры

| # | Компонент | Угроза | STRIDE | Меры |
|---|---|---|---|---|
| T1 | Сенсор | Подделка X-Forwarded-For, чтобы заблокировать IP легитимных пользователей или партнёров | S, D | Доверять заголовку только от заданных прокси; блокировка по составному отпечатку; allowlist; ограниченный TTL блокировок; правило Semgrep запрещает читать такие заголовки напрямую |
| T2 | Сенсор | Межсайтовое срабатывание: страница атакующего заставляет браузер жертвы запросить приманку с её cookies, аккаунт жертвы замораживается | S, D | Учёт Sec-Fetch-Site; приманки высокой уверенности требуют CORS preflight (нестандартный заголовок или JSON); для аутентифицированных — реакция только по накопленному скорингу |
| T3 | Сенсор | Перегрузка сенсора для отключения защиты | D | Fail-open; лимиты памяти и соединений; таймауты; неблокирующая отправка событий; алерт при переходе в fail-open |
| T4 | Сенсор | Утечка паролей и персональных данных в логи и события | I | Только метаданные; запрет логирования тел, заголовков авторизации, cookies и параметров запроса — правила Semgrep для Go и Python с отслеживанием потока данных; маскирование |
| T5 | Сенсор | Распознавание продукта по характерным ответам и обход приманок | I | Без баннеров и уникальных заголовков; рандомизация и ротация приманок; приманки в стиле стека клиента; исходный код публичен, поэтому шаблоны приманок и сид генерации не хранятся в репозитории и уникальны для каждого клиента (ADR-0011) |
| T6 | Сенсор ↔ control plane | Взломанный control plane раздаёт вредоносную конфигурацию | T, E | Подпись конфигурации ключом вне сервера раздачи (KMS/офлайн); проверка подписи сенсором; mTLS |
| T7 | Сенсор | Подделка canary-токенов, чтобы вызвать ложные срабатывания на чужие сессии | S | HMAC-подпись токена; привязка к сессии выдачи; ротация ключей; правило Semgrep требует сравнения подписи за постоянное время |
| T8 | Дашборд | XSS через данные атакующего (User-Agent, путь, параметры) | T, E | Экранирование вывода; строгий CSP; данные атакующего только как текст |
| T9 | Алерты | Подделка нажатий кнопок в Telegram | S, E | Проверка отправителя callback; подписанные одноразовые callback-данные с коротким сроком жизни |
| T10 | Администрирование | Невозможно доказать, кто выполнил действие (разбан, разморозка) | R | Журнал аудита только на добавление |
| T11 | Red-team валидатор | Использование продукта для атаки чужих ресурсов | E, D | Подтверждение владения доменом; лимиты скорости; аудит запусков; изолированная сеть |
| T12 | CI/CD | Подмена зависимости или GitHub Action; выполнение кода из PR с правами репозитория; инъекция команд через данные события (заголовок PR, имя ветки) | T, E | Actions по SHA; минимальные права GITHUB_TOKEN; только `pull_request`, не `pull_request_target`; токен не сохраняется после checkout; данные событий только через env; actionlint и правила Semgrep для workflow; lock-файлы и go.sum для инструментов; Dependabot с выдержкой 7 дней; gitleaks; сканеры закреплены по digest и работают без сети; SBOM; подпись образов cosign (ADR-0015, ADR-0016) |
| T13 | Хранилище | Избыточное хранение персональных данных (IP) | I | Минимизация; срок хранения по умолчанию 90 дней; удаление партициями |
| T14 | Инфраструктура | Сторонние образы (NATS, PostgreSQL) с небезопасными настройками по умолчанию: опубликованные пароли, лишние открытые порты, запуск от root | E, I | Своя конфигурация вместо файла из образа; явный non-root пользователь; изолированная сеть без выхода в интернет; проверка политики контейнеров в `make lint` (ADR-0014) |

## Принятые риски

- **R1.** В режиме fail-open обнаружение отключено. Принимаем ради доступности
  приложения клиента, компенсируем алертом о переходе в обход (T3).
- **R2.** При переполнении буфера события теряются. Принимаем, чтобы не замедлять
  приложение; потери учитываются счётчиком и видны в мониторинге.
- **R3.** Атакующий, который знает реальную уязвимость и идёт к ней напрямую,
  не касаясь приманок, обнаружен не будет. Продукт — слой обнаружения,
  а не замена WAF, пентеста и безопасной разработки.
- **R4.** Исходный код продукта публичен, поэтому атакующий может изучить
  логику обнаружения. Принимаем ради бесплатных проверок безопасности
  GitHub и ради того, чтобы стойкость не опиралась на секретность кода
  (ADR-0011). Компенсируется тем, что конкретные приманки генерируются
  под клиента и в репозитории не хранятся.
- **R5.** В базовых образах бывают уязвимости, для которых исправление ещё
  не выпущено. Проверка Trivy на них не падает — обновиться не на что, —
  но показывает их в сводке. Сейчас это 44 уязвимости уровня HIGH в пакетах
  Debian образа control plane; наш код эти пакеты не использует. Снижается
  переходом control plane на distroless (технический долг) и еженедельным
  прогоном, который покажет, когда исправление появится (ADR-0016).
