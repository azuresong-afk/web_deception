# Вспомогательные скрипты

Скрипты для сборки, генерации и локальных проверок, которые вызываются
из `Makefile` и pre-commit хуков. Держим их здесь, чтобы Makefile оставался
списком целей, а не сборником shell-однострочников.
