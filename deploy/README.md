# Развёртывание

- `docker/` — Dockerfile сенсора и control plane (multi-stage, non-root).
- `compose/` — docker compose для локальной разработки: весь стек одной командой.

Helm-чарты и продакшен-манифесты появятся на этапе 10.

Появится на шаге 4 этапа 1.
