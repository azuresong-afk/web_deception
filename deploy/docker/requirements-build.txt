# Инструменты, нужные только на этапе сборки образа control plane.
# Устанавливаются командой pip install --require-hashes.
#
# Зачем контрольные суммы: pip без них скачает то, что реестр отдаст
# под этим номером версии. С ними — только файл с точно таким содержимым,
# а любой другой будет отвергнут. Перечислены суммы двух колёс: для amd64
# и для arm64, чтобы образ собирался и в CI, и на Mac с Apple Silicon.
#
# Почему uv ставится через pip, а не копируется из официального образа
# ghcr.io/astral-sh/uv: так мы доверяем на один реестр образов меньше.
# PyPI нам нужен в любом случае — оттуда приходят все зависимости.
uv==0.12.17 \
    --hash=sha256:9e25bb39e1674799c408345a6397ebc2c7c719d498be0ce9d935466d36ceacf5 \
    --hash=sha256:e2bc5ec70e395fc5769becd1b81f6cedc66d23ccaf1392743f0f8c9cfe81b18c
